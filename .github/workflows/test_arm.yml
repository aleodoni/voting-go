name: Test Native ARM Runner

on:
  workflow_dispatch:

jobs:
  test-arm-runner:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Show architecture
        run: |
          uname -a
          uname -m

      - name: CPU info
        run: |
          lscpu